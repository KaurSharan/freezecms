<table class="table table-hover table-bordered">
    <thead>
      <tr>
        <th>S.no</th>
        <th>Page Title</th>
        <th>Show</th>
        <th>Edit</th>
        <th>Delete</th>
      </tr>
    </thead>
    <tbody>
      <% @page.each do |p| %>
      <tr>
          <td>1</td>
        <td><%= p.title %></td>
        <td><%= link_to(image_tag("show.png", :border=>0),{:action=>'show',:id => p.id})%></td></td>
        <td><%= link_to(image_tag("edit.gif", :border=>0),{:action => 'edit',:id => p.id})%></td></td>
        <td><%= link_to(image_tag("delete.gif", :border=>0),{:action => 'delete',:id => p.id})%></td>
      </tr>
      <% end %>
    </tbody>
  </table>