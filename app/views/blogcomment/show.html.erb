<table class="table table-bordered">
	<thead>
			<tr>
				<td>Id</td>
				<td>Blog</td>
				<td>Comment</td>
			</tr>
	</thead>
			<tr class="danger">
				<td><%= @us.id %></td>
				<td><%= @us.blog.title %></td>
				<td><%= @us.comment %></td>
			</tr>
			<%= button_to('Back to view page', :action=>'view', :class => 'btn btn-primary')  %>
				

</table>